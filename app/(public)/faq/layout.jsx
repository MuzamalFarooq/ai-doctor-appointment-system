export const metadata = {
  title: 'FAQ - MediConnect AI',
  description: 'Frequently asked questions about MediConnect AI healthcare platform.',
};

export default function FAQLayout({ children }) {
  return children;
}
